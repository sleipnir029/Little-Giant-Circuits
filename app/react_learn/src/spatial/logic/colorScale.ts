// Placeholder — Step B will bind activation intensity here.
// For Step A every component uses its base colour at full opacity.
export function activationColor(_componentId: string, _intensity: number): string {
  return '#ffffff'
}
